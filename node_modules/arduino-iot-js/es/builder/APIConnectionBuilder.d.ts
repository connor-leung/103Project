import { MqttClient } from 'mqtt';
import { IHttpClient } from '../http/IHttpClient';
import { IConnection } from '../connection/IConnection';
import { IConnectionBuilder } from './IConnectionBuilder';
import { APIOptions, CloudOptions, BaseCloudOptions } from '../client/ICloudClient';
export declare class APIConnectionBuilder implements IConnectionBuilder {
    private client;
    private mqttConnect;
    constructor(client: IHttpClient, mqttConnect: (string: any, IClientOptions: any) => MqttClient);
    canBuild(options: CloudOptions): boolean;
    build(options: APIOptions & BaseCloudOptions): Promise<IConnection>;
}
